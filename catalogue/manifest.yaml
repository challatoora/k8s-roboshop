apiVersion: v1
kind: ConfigMap
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
    component: catalogue
    tier: be
data:
  MONGO: "true"
  MONGO_URL: "mongodb://mongodb:27017/catalogue"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
    component: catalogue
    tier: be

spec:
  # modify replicas according to your case
  replicas: 1
  selector:
    matchLabels:
      project: roboshop
      component: catalogue
      tier: be
   
  template:
    metadata:
      labels:
        project: roboshop
        component: catalogue
        tier: be
    spec:
      containers:
      - name: catalogue
        image: challatoor/catalogue:1.0.0
        envFrom:
        - configMapRef:
            name: catalogue

---
apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
  labels:
        project: roboshop
        component: catalogue
        tier: be
  
spec:
  selector:
    #lables
      project: roboshop
      component: catalogue
      tier: be
  ports:
  - protocol: TCP
    port: 8080 #vice port
    targetPort: 8080 #tainer port
